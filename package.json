{
  "name": "dulkangs-corner",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "build": "node build.js",
    "deploy": "npm run build && gh-pages -d dist",
    "publish": "git add . && git commit -m \"New blog post\" && git push origin main && npm run deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ejs": "^3.1.10",
    "express": "^5.2.1",
    "front-matter": "^4.0.2",
    "marked": "^17.0.1"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}